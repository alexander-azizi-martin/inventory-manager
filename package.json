{
  "name": "inventory-manager",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "yarn workspaces run build"
  },
  "devDependencies": {
    "prettier": "^2.1.2"
  },
  "engines": {
    "yarn": "1.x"
  }
}
